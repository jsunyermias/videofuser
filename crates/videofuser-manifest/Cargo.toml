[package]
name = "videofuser-manifest"
version = "0.1.0"
edition = "2021"
description = "Parsing and generation of the four manifest formats (ebml, nfo, xml, md)"

[dependencies]
byteorder     = { workspace = true }
thiserror     = "1"
