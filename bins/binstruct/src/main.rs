// binstruct tool: gen / inspect / verify / verify-manifest subcommands.
// Fase 3: full implementation.
fn main() {
    eprintln!("binstruct: not yet implemented (Fase 3)");
    std::process::exit(1);
}
